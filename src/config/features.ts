export const features = {
  /** Online simulator slot booking (widget, nav links, API). */
  bookingSlots: false,
  /** Shop catalog and product listings. */
  shop: false,
  /** Cafe menu and lounge food section. */
  cafe: false,
  /** Testimonials page and nav links. */
  testimonials: false,
  /** Blog page and nav links. */
  blog: false,
} as const;
